spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASS}

um:
  warsaw:
    api-key: ${UM_WARSAW_API_KEY}

logging:
  level:
    pl.nextleveldev.smart_route: debug